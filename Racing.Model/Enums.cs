namespace Racing.Model
{
    // Enum for vehicle type
    public enum VehicleType
    {
        Truck,
        Car
    }

    // Enum for response message
    public enum ResponseMessage
    {
        None,
        Inserted,
        Deleted,
        Overloaded,
        InspectionFail
    }
}
